#Ejercicio 7.2

import time

hora = time.strftime('%H')
minuto = time.strftime('%M')

if(int(hora) >= 19):
    print("Hora de ir a casa")
else:
    print("Faltan {} horas y {} minutos para ir a casa".format(18- int(hora), 59-int(minuto)))
